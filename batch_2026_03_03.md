# Batch: Union Quality, Export/Print, NLRB Sync (2026-03-03)

## Task 7-4: Filter/Flag Stale Union Records
- Added `is_likely_inactive BOOLEAN DEFAULT FALSE` to `unions_master`
- 6,053 of 26,693 unions flagged (yr_covered < 2022)
- Partial index `idx_unions_master_inactive`
- API: `include_inactive` param on `/api/unions/national` endpoints
- Frontend: "(Inactive)" badge in AffiliationTree, amber banner on UnionProfilePage
- Script: `scripts/etl/flag_stale_unions.py --commit`
- Tests: 4 backend, 2 frontend

## Task 7-1: Union Hierarchy Name Parsing
- `_classify_union_level()` helper using `desig_name` codes
- `INTERMEDIATE_CODES = {'DC', 'JC', 'CONF', 'D', 'C', 'SC', 'SA', 'BCTC'}`
- New endpoint: `GET /api/unions/hierarchy/{aff_abbr}` (returns national, intermediates with nested locals, orphan locals by state)
- Added `parent_fnum VARCHAR` to `unions_master`
- Frontend: `IntermediateNode` component, 4-level tree (Affiliation -> Intermediate -> State -> Local)
- Script: `scripts/etl/relink_orphan_locals.py --commit`
- Tests: 10 backend, 3 frontend
- Gotcha: `desig_num` column does NOT exist -- only `desig_name`

## Task 6-1: Enhanced CSV Export
- `GET /api/scorecard/unified/export` -- StreamingResponse, 33 columns, 10K cap
- Route placed BEFORE `/{employer_id}` to avoid conflict
- Profile CSV expanded from 12 to 33 fields with date stamp
- Export button on UnifiedScorecardPage with Download icon
- `buildExportUrl()` in `frontend/src/shared/api/scorecard.js`
- Tests: 5 backend, 1 frontend

## Task 6-2: PDF/Print Profiles
- `@media print` CSS in `index.css`: hides nav/buttons, resets colors, forces collapsible open
- `data-no-print` on NavBar, Layout breadcrumbs
- `print-section` class on CollapsibleCard, `data-collapsible-content` attribute
- Print Profile button with Printer icon in ProfileActionButtons
- Tests: 2 frontend (ProfileActionButtons), 1 frontend (NavBar data-no-print)

## Task 5-1: NLRB Data Sync from SQLite
- `scripts/etl/sync_nlrb_sqlite.py` -- 10-phase diff-based sync
- Phases: cases, filings, elections, election_results, participants, tallies, docket, allegations, voting_units, sought_units
- Key deltas: ~180 elections, ~232K participants, ~24K cases, ~8K docket entries
- Dedup key lessons: elections use (case_number, election_date) not ballot_type; tallies use (case_number, labor_org_name, votes_for); participants normalize junk city values
- Elections phase also backfills NULL ballot_type from SQLite
- Tests: 13 backend (10 prereq table checks + 3 script tests)
