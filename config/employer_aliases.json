{
  "_doc": "Employer name aliases + collision exclusions + canonical-row preference for unified employer search. Loaded by api/routers/employers.py::_load_aliases(). When the user's query matches one of an entry's aliases: (a) `exclude_terms` add WHERE LOWER(search_name) NOT LIKE %s guards; (b) `canonical_patterns` add an ORDER BY boost so search_name LIKE the canonical pattern wins regardless of trigram score (R7-7 heavy, 2026-05-03 -- needed for flat-MASTER fragments where 'walmart' per-store rows have similarity=1.0 vs 'walmart inc.' canonical at 0.667). Fail-open if this file is missing or malformed.",
  "aliases": [
    {
      "canonical_name": "Cleveland Clinic Foundation",
      "aliases": ["cleveland clinic", "ccf"],
      "exclude_terms": ["cleveland-cliffs", "cleveland cliffs"],
      "canonical_patterns": ["cleveland clinic"],
      "notes": "Trigram similarity ranks Cleveland-Cliffs Inc (steel/mining) high on 'cleveland clinic' queries because of the shared prefix. Distinct entities."
    },
    {
      "canonical_name": "New York City Health and Hospitals Corporation",
      "aliases": ["nyc hospitals", "nyc hospital", "nyc health and hospitals", "nyc health + hospitals", "nyc hhc", "h+h"],
      "exclude_terms": ["nyu langone", "nyu hospital", "nyu medical", "hotel trades"],
      "canonical_patterns": ["new york city health and hospitals"],
      "notes": "R8-7 (2026-06-07): added 'nyc hospitals'/'nyc hospital' aliases -- the audit query was 'NYC Hospitals' which matched NO prior alias, so the entry never fired and generic '*hospitals' rows (SHOALS HOSPITAL, NYU LANGONE) won. canonical_pattern tightened to 'new york city health and hospitals' (the real MASTER canonical search_name) -- the looser 'nyc health'/'new york city health' patterns also matched a NY-hotel-trades 'health center' row (sz=1000) that outranked the real canonical (sz=390); 'hotel trades' added to exclude_terms. NYU Langone is a separate private system."
    },
    {
      "canonical_name": "Health Care Service Corporation",
      "aliases": ["hcsc"],
      "canonical_patterns": ["health care service corp"],
      "notes": "Multi-state Blue Cross plan. The acronym 'HCSC' is a known organizer-target lookup; the trigram score for 'hcsc' against the long form is low so users often miss it."
    },
    {
      "canonical_name": "Walmart Inc",
      "aliases": ["walmart", "wal-mart", "walmart stores"],
      "canonical_patterns": ["walmart inc", "wal-mart inc", "wal-mart stores inc"],
      "notes": "Flat-MASTER fragments ('walmart' alone, similarity=1.0, 25K-25K-employee per-store rows) drown out the canonical 'walmart inc.' (similarity=0.667, unit_size=10M). canonical_patterns boost pushes the parent to the top regardless of trigram score."
    },
    {
      "canonical_name": "Amazon.com Inc",
      "aliases": ["amazon"],
      "canonical_patterns": ["amazon.com inc", "amazon com inc", "amazon inc"],
      "notes": "Same flat-MASTER pattern as Walmart -- 'amazon' per-warehouse fragments at similarity=1.0 beat Amazon.com Inc parent."
    },
    {
      "canonical_name": "Target Corporation",
      "aliases": ["target", "target corp"],
      "canonical_patterns": ["target corporation", "target corp"],
      "notes": "Per-store Target rows beat parent on 'target' trigram. Same fix."
    },
    {
      "canonical_name": "The Kroger Co",
      "aliases": ["kroger", "the kroger company"],
      "canonical_patterns": ["the kroger co", "kroger co", "kroger company"],
      "notes": "Per-store Kroger fragments (similarity=1.0 on 'kroger') drown out parent."
    },
    {
      "canonical_name": "Starbucks Corporation",
      "aliases": ["starbucks", "starbucks coffee"],
      "notes": "No canonical_patterns: the 2026-04-23 family rollup put the Starbucks F7 canonical-group leader (search_name='starbucks', cw=119) above flat MASTER per-store rows already. Adding a 'starbucks corp' pattern boost would demote that F7 leader behind WA-state-prefixed MASTER rows (e.g. 'wa317942390 - starbucks corp')."
    },
    {
      "canonical_name": "General Electric Company",
      "aliases": ["ge", "general electric"],
      "exclude_terms": ["ges"],
      "canonical_patterns": ["general electric"],
      "notes": "R8-7 (2026-06-07): 'ge' acronym -> 'general electric' trigram similarity=0.11, far below the 0.3 gate, so the canonical was filtered out and 'GES' (Dallas) won. The 'ge' alias is <=3 chars so it requires an EXACT-match query ('ge' only, not substrings of 'general'/'george'); the canonical_pattern is OR'd into the gate to pull General Electric Company into the candidate set."
    },
    {
      "canonical_name": "General Motors LLC",
      "aliases": ["gm", "general motors"],
      "exclude_terms": ["gm-spo"],
      "canonical_patterns": ["general motors"],
      "notes": "R8-7 (2026-06-07): 'gm' -> 'general motors' trigram similarity=0.06; the NLRB filer 'GM-SPO' (sim 0.43) won instead. 'gm' is <=3 chars so EXACT-match guarded; canonical_pattern injected into the gate."
    },
    {
      "canonical_name": "The Procter & Gamble Company",
      "aliases": ["p&g", "procter", "procter and gamble", "procter & gamble"],
      "exclude_terms": ["pse&g", "p&g power"],
      "canonical_patterns": ["procter & gamble", "procter and gamble"],
      "notes": "R8-7 (2026-06-07): 'p&g' -> 'procter & gamble' trigram similarity=0.12; the utility 'PSE&G' (sim 0.43) won. canonical_patterns (both '&' and 'and' spellings -- DB stores 'procter & gamble') OR'd into the gate; exclude_terms drop PSE&G and the unrelated 'P&G Power Co'. Georgia-Pacific 'G-P' rows never enter via the procter pattern, so no exclude needed there."
    },
    {
      "canonical_name": "Kaiser Permanente",
      "aliases": ["kp", "kaiser permanente", "kaiser foundation"],
      "canonical_patterns": ["kaiser permanente", "kaiser foundation"],
      "notes": "R8-7 (2026-06-07): 'kp' -> 'kaiser permanente' trigram similarity=0.05; 'KP, LLC' (WA) and 'KPEP' (MI) won. 'kp' is <=3 chars so EXACT-match guarded. No exclude_terms needed -- the Kaiser F7 leader (search_name='kaiser permanente', cw=162K) sorts to rank 1 once injected, like Starbucks."
    }
  ]
}
